"use client";
import {useEffect} from 'react';
import gsap from 'gsap';
import {ScrollTrigger} from 'gsap/ScrollTrigger';
export default function Animations(){useEffect(()=>{gsap.registerPlugin(ScrollTrigger);const mm=gsap.matchMedia();mm.add('(prefers-reduced-motion: no-preference)',()=>{const ctx=gsap.context(()=>{gsap.from('.hero-copy > *',{y:22,opacity:0,stagger:.08,duration:.65,ease:'power3.out',clearProps:'transform,opacity'});gsap.utils.toArray<HTMLElement>('.reveal').forEach(el=>gsap.from(el,{y:30,clipPath:'inset(8% 0 0 0)',duration:.65,ease:'power3.out',scrollTrigger:{trigger:el,start:'top 94%',once:true},clearProps:'transform,clipPath'}));gsap.to('.hero-art > .architecture',{y:45,rotation:2,ease:'none',scrollTrigger:{trigger:'.hero',start:'top top',end:'bottom top',scrub:1}});gsap.from('.timeline',{ '--line-progress':'0%',ease:'none',scrollTrigger:{trigger:'.timeline',start:'top 75%',end:'bottom 70%',scrub:1}})});return()=>ctx.revert()});const progress=document.getElementById('scroll-progress');const sections=Array.from(document.querySelectorAll<HTMLElement>('#home,#projects,#experience,#skills,#about'));const links=Array.from(document.querySelectorAll<HTMLAnchorElement>('.nav nav a'));let frame=0;const update=()=>{const max=document.documentElement.scrollHeight-innerHeight;if(progress)progress.style.transform=`scaleX(${max>0?scrollY/max:0})`;let current='home';let best=-Infinity;sections.forEach(s=>{const y=s.getBoundingClientRect().top;if(y<=160&&y>best){best=y;current=s.id}});links.forEach(a=>{const active=a.hash==='#'+current;a.classList.toggle('active',active);if(active)a.setAttribute('aria-current','location');else a.removeAttribute('aria-current')});frame=0};const onScroll=()=>{if(!frame)frame=requestAnimationFrame(update)};window.addEventListener('scroll',onScroll,{passive:true});update();return()=>{mm.revert();window.removeEventListener('scroll',onScroll);cancelAnimationFrame(frame)}},[]);return <div id="scroll-progress" aria-hidden="true"/>}
